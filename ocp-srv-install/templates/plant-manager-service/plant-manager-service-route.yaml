apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: plant-manager-service
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: plant-manager-service
    weight: 100
  wildcardPolicy: None