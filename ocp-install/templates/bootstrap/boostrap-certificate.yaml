
---
apiVersion: v1
data:
  password: cGFzc3dvcmQ=
kind: Secret
metadata:
  name: keystore-secret-bootstrap
type: Opaque

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bootstrap
spec:
  commonName: bootstrap.manufacturing-dev.qiot-project.io
  dnsNames:
  - bootstrap.manufacturing-dev.qiot-project.io
  issuerRef:
    name: ocp-install-issuer-vault
  keystores:
    pkcs12:
      create: true
      passwordSecretRef:
        key: password
        name: keystore-secret-bootstrap
  secretName: bootstrap-certificate